# Evaluate expression with meaningful message

Method created for the `FilteredData` object to execute filter call with
meaningful message. After evaluation used environment should contain all
necessary bindings.

## Usage

``` r
eval_expr_with_msg(expr, env)
```

## Arguments

- expr:

  (`language`)

- env:

  (`environment`) where expression is evaluated.

## Value

`NULL`, invisibly.
